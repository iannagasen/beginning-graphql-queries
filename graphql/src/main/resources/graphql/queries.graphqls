type Query {
  customerById(id: ID): Customer
  customers: [Customer]
  customersReactive: [Customer]
  helloWithName(name: String): String
  hello: String
}

type Customer {
  id: ID
  name: String
  account: Account
}

type Account {
  id: ID
}
